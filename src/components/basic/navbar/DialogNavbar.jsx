import { Dialog } from "@mui/material"


const DialogNavbar = ({ open, onClose }) => {
    return (
        <Dialog open={open} onClose={onClose}>
            hgjh
        </Dialog>
    )
}

export default DialogNavbar